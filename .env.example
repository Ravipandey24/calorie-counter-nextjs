# Backend API Configuration
# Base URL for the backend API (required)
NEXT_PUBLIC_API_BASE_URL=http://localhost:3001

# JWT Authentication Configuration  
# JWT secret key for server-side token verification (required)
# Must be at least 32 characters long and match your backend JWT secret
# Generate with: openssl rand -base64 32
JWT_SECRET=your-super-secure-jwt-secret-key-minimum-32-characters-long

# Optional Development Settings
# Skip environment validation during build (useful for CI/CD)
# SKIP_ENV_VALIDATION=true

# Example production values (DO NOT USE IN PRODUCTION):
# NEXT_PUBLIC_API_BASE_URL=https://your-api-domain.com
# JWT_SECRET=<your-production-jwt-secret-from-backend>
